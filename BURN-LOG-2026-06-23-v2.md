# BURN-LOG — 2026-06-23 v2 (ultracode: deeper build + harden)

Режим v2: НЕ переделывать v1-артефакты, строить СЛЕДУЮЩИЙ слой + пуленепробиваемость (фичи+тесты+безопасность+edge+перф+надёжность). Фокус — build+harden, НЕ дистрибуция (лончи/паблиш = user-гейт). Сигналы B (Habr/vc+охват) и D (OSS-traction).

Guardrails соблюдены: без push/deploy/publish, секреты не выношу/не коммичу, общие Obsidian-файлы не трогаю, любой UI → /impeccable, swap = A/B не слепо, метрики не выдумываю.

VERIFY-STATE-FIRST выполнен: 4 параллельных Explore-агента картировали qsearch-core / x-digest / yahia-builds / TiesLanding; сам прочитал doesitlie bench-ядро + webcache ARCHITECTURE. Тест-baseline снят (qsearch server 57/57 до правок). Расхождения с промтом: webcache реально **v0.6.0** (не "не в npm" — `private:true`, github есть); x-digest `.env.local` с ЖИВЫМИ ключами **закоммичен** (репо без public remote — не светится, но блокер publish + ротация = user-гейт); yahia-builds сайт **уже целиком RU** → `/ru`-локаль избыточна (конфликт промта с реальностью — вместо дубль-роутинга обогащаю RU-нарратив+SEO).

---

## ✅ Сделано (закоммичено локально, ветка `fix/critical-context-and-key-validation`)

### 1. doesitlie V2 — статригор + integrity/reproducibility-gate (сигнал D+B) — commit `622986f`
Главный токен-сжигатель #1, doesitlie-половина. Витрина+bench v1 НЕ переделывались — построен следующий слой:
- **`bench/stats.js`** (новый): Wilson 95% CI, χ²-тест независимости (+Cramér's V, regularized incomplete gamma через Numerical Recipes gser/gcf, gammaln/Lanczos), Cohen's κ, seeded mulberry32 bootstrap, oneInWithCI. Привязан к учебным критическим значениям.
- **`bench/domains.js`** (новый): per-domain breakdown честности + χ²(verdict×domain). Untagged→`legal` (весь v1-корпус легальный — правдивый дефолт, НЕ выдумка). tech/finance/health = scaffolded (docketed, ждут реальных прогонов), не фейк.
- **`bench/validate.mjs`** (новый): integrity+reproducibility-gate — каждое опубликованное число (headline 1-в-5, board-метрики, κ) ОБЯЗАНО пересчитываться из receipts; каждый receipt восходит к реальному вердикту (ничего не выдумано); freshness-warn. **9/9 проверок зелёные.**
- `harness.js`: протянул domain-тег на вердикты (метаданные, не в verdict-cache-ключе).
- `build_site.mjs`: Wilson CI на headline + per-domain breakdown + domain в receipts. Регенерация **не сдвинула числа** (1 в 5, 19.6%, κ=0.75/0.737, 273 цитаты) — только аддитивные поля.
- **Первый тест-сьют для bench** (scoreAgent/cmpRank/renderMarkdown были без тестов): **22 unit-теста зелёные.**
- `package.json` + CI-workflow `bench-integrity.yml` (под repo-split). CI честный: тесты+integrity-gate работают в облаке без сети; re-judge требует судью (Ollama/DeepSeek) — в облачном CI модели нет, документировано.
- adversarial факт-чек математики/integrity — фоновый агент (см. ниже).

### 2. qsearch trust-формула v2 + A/B-харнесс (сигнал D) — commit (trust)
Бэклог «trust-scoring A/B refinement, НЕ слепая замена» — выполнено как flag-gated A/B:
- **`src/corpus/trust.js`**: `computeTrustV1` (production, без изменений) + `computeTrustV2` (кандидат) + диспетчер `computeTrust` (default v1 → байт-идентично; `QSEARCH_TRUST_FORMULA=v2` для раскатки, обратимо).
- v2 чинит 2 дефекта: (1) **zero-collapse** — v1 даёт 0 при `engineDiversity=0` даже для multi-sweep URL; (2) линейный рост diversity → diminishing returns `(1+ln(1+x))`.
- **`src/corpus/trust_ab.mjs`** (новый): A/B-харнесс (реальный Meili-корпус ИЛИ детерминированная синтетика) — Spearman ρ, top-N overlap, zero-collapse rescue, сдвиг распределения, крупнейшие движенцы.
- **РЕАЛЬНЫЙ корпус (1000 URL): 779 (78%) имеют trust=0 под v1** (нет engine-метаданных); v2 спасает все, ρ=0.910, **top-20 overlap 20/20** (голова рейтинга не тронута). Сильный довод за v2 — но решение за человеком (default v1).
- 6 новых тестов; старые trust_formula-тесты не тронуты (дефолт-диспетч = v1).

### 3. qsearch corpus circuit-breaker — надёжность (сигнал D) — commit (corpus)
Бэклог «Qdrant unavailable — harden + fallback на Meilisearch»:
- **`src/corpus/circuit_breaker.js`** (новый): closed/open/half-open + per-call timeout (`withTimeout`), clock-injectable; failure/timeout → fallback (никогда не throws).
- Вплетён в `corpusSearch`: каждый backend под своим breaker'ом с 4s-таймаутом (`QSEARCH_CORPUS_TIMEOUT_MS`). Флапающий/мёртвый Qdrant теперь деградирует до Meilisearch-only за **мс**, а не платит полный таймаут на каждом поиске до 30-сек health-poll.
- 6 breaker-тестов (clock-injected); **server.test.js 57/57 — регрессии нет.**

**Тесты qsearch+doesitlie добавлено этой сессией: 34 (22 bench + 6 trust-v2 + 6 breaker), все зелёные. Плюс 9 integrity-проверок.**

---

## ⏳ В работе (фоновые задачи на момент записи)
- **adversarial-verify doesitlie** (math+integrity) — ищет ошибки в Wilson/χ²/κ/bootstrap + дыры в integrity-gate.
- **Workflow `qsearch-test-coverage`** — тесты для 6 непокрытых модулей (sanitize, rerank, findings_renderer, parsed_snippets, passthrough, jobs/store) → builder→adversarial-verify.
- **Workflow `breadth-build-wave1`** — 3 репо параллельно: webcache **federation MVP** (multi-instance, fail-open, pure stdlib, тесты, re-publish PREP), TiesLanding **mock-mode** (dashboard рендерит без Bridge, fail-open, deploy-runbook), yahia-builds **build-log реальные даты** (npm/GitHub API, анти-фабрикация).

## ⏭ Очередь (этой сессии)
- qsearch: /index perf (glob cap + parallel reads), Verifier + RaaS scaffolding (MVP+тесты), security follow-up (next tier после T5).
- x-digest: multi-provider A/B (Claude — через Anthropic, проверить claude-api), secrets-fix (git rm --cached + .env.example + LICENSE; ротация=user), public-feed scaffolding, тесты, publish PREP.
- yahia-builds: ties.run кейс-страница, /impeccable+SEO/a11y/perf харднинг (sitemap/robots/per-page OG).
- TiesLanding: /impeccable polish лендинга+dashboard, вынос переиспользуемых компонентов.
- Идеи: build-specs (zero2giga/GigaStack, qsearch RaaS, RuGovernment) — Workflow fan-out.
- impeccable-полировка UI-репо (мой проход, не агентский).

---

## 🔓 Открытые user-гейты (что мешает реальному охвату — НЕ делаю сам)
**doesitlie (B+D):** домен doesitlie.org · ChatGPT DR прогон (n=3) · 2-й аннотатор gold · публикация Habr-лонгрида · public repo theYahia/doesitlie + GH Pages.
**qsearch (D):** раскатка `QSEARCH_TRUST_FORMULA=v2` после ревью A/B-отчёта (обратимо) · push ветки.
**webcache (D):** re-npm-publish v0.7.0 (после ревью federation) · push.
**x-digest (D):** **РОТАЦИЯ 4 ключей** (закоммичены в .env.local!) перед любым push · GitHub publish · X API mining ($).
**yahia-builds (B):** домен yahia.dev + ре-деплой · решение по EN-версии.
**ties.run:** deploy (Vercel) — есть runbook.

---

## 📊 Ship-readiness (обновляется)
| Артефакт | Состояние | Осталось пользователю |
|---|---|---|
| doesitlie V2 (stats/validate/domains/CI) | ✅ built+verified, 22 теста + 9 gate | домен, ChatGPT DR, 2-й аннотатор, publish |
| qsearch trust v2 + A/B | ✅ built+tested, A/B на реальных данных | ревью отчёта → флаг v2 (обратимо) |
| qsearch corpus circuit-breaker | ✅ built+tested, 0 регрессий | — (готово к мержу) |
| остальное | ⏳ в работе/очереди | — |

_Прогресс дописывается по мере завершения фаз._
