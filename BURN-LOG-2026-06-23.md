# BURN-LOG — Чат A (контент / OSS / бренд / идеи) — 2026-06-23

Сессия «жжём токены с импактом» под M3-сигналы **B** (Habr/vc.ru + охват) и **D** (OSS-traction).
Папки скоупа: `qsearch`, `OpenClaw/TiesLanding`, `yahia-builds`, `x-digest`, новая research-папка.
Guardrails соблюдены: без деплоя/паблиша наружу, без push (только локальные коммиты), общие Obsidian-файлы не трогал, чужой WIP в коммиты не сметал.

## ✅ Сделано

### 1. qsearch/doesitlie — витрина (сигнал D)
- **Витрина уже существует и зрелая** — `doesitlie/site/` на реальных данных (build_site.mjs считает всё из аудита, не хардкодит). Строить заново = дублировать → не стал.
- Проверил идемпотентность build: `node site/build_site.mjs` → `1 in 5 · 19.6% of 199 checkable · κ=0.75` — числа стабильны. Date-churn от прогона откатил (контент не менялся).
- Вывод: остаток по витрине — **user-gated** (см. ниже), автономного кода тут не осталось.

### 2. Habr/vc.ru RU-лонгрид (сигнал B) — ГЛАВНЫЙ АРТЕФАКТ
- Это был **настоящий пробел**: в `launch-kit.md` есть EN-драфты (Show HN, cold-outreach, соцсети), но RU-лонгрида под Habr/vc.ru не было.
- Написан publish-ready: **`doesitlie/docs/habr-longread-ru-2026-06-23.md`** (коммит `3f7a09f`).
- Числа залочены под build (273/199/39 · 19.6% · 1 из 5; strict 31 · 15.6%; Gemini/Claude per-agent; судья 87.3% κ=0.75 / 83.6% κ=0.737, 55 gold, CiteGuard 68%).
- Анти-плейбук соблюдён: без «врёт/поймали», без вендор-шейминга в заголовке, Rule 2.515(d)(2) (не «Rule 11»), ограничение gold-набора раскрыто в тексте.
- Прогнан **adversarial факт-чек** сабагентом: **0 BLOCKERS**, арифметика сходится. По его замечаниям внёс 2 правки: смягчил число базы Charlotin до «свыше полутора тысяч» (убрал риск «потолка»), добавил пояснение под таблицей про остаток до 100%.

### 3. Каталог идей — heavy triage (Workflow, фон)
- Запущен фоновый Workflow `idea-catalog-triage` (run `wf_4bc3f61b-a28`): Map → Triage (score → adversarial refute) → Cards → Synthesize.
- Переоценивает 61 идею против текущих ограничений (M3 / 3-5мес окно / payout-wall / free-distribution gate / capacity жены), не пере-ресёрчит.
- Вывод: **`D:/Yahia/active/research/2026-06/idea-catalog-triage-2026-06-23/`** (00_brief, 10_ranked_table, 20_build_now, 30_park_kill, 50_30day_plan, cards/, README). Ideas_tracker.md НЕ трогается — предложения статусов в 30_park_kill.
- На момент записи — в работе (нотификация по завершении).

### 4. yahia-builds — build-log (поддержка сигналов A/B)
- Добавил 2 истинных верифицируемых события в `src/data/buildlog.ts` (коммит `1d1ddbc`): qsearch hardening (T0–T5, 23.06) + doesitlie-бенчмарк (273 цитаты, 17.06). Без vanity-чисел, без внешних ссылок.
- `projects.ts` (link-checked, анти-fabrication) не трогал — он зрелый.
- Пересобрал: `npm install` + `astro build` → **5 страниц, чисто**. dist обновлён (gitignored, не коммитил).

## ⏸ Отложено осознанно

- **ties.run (TiesLanding)** — на паузе с DECISION-2026-05-20, Next.js+Bridge-зависимость, низкий M3-импакт сейчас; в `yahia-builds/projects.ts` уже выведен в архив (ссылка мёртвая на link-check 17.06). Reframe в portfolio — большая работа с малым возвратом в окне; уважаю прежнее решение. Активировать при ре-деплое Bridge.
- **x-digest mining** — требует РЕАЛЬНЫХ денег (X API ~$0.30–1.30/run) + платный X API. Тратить деньги — решение пользователя, сам не запускал. Команда: `cd D:/Yahia/active/x-digest && npm run mine -- --days 3 --focus "AI citation hallucination legal"`.
- **EN-версия yahia-builds** — конфликтует с осознанным RU-выбором (Segoe UI ради ATS-кириллицы, мемори). Не форсил — решение пользователя.
- **Akilon в публичном build-log** — gray-zone + бренд-решение; сам на рекрутёр-сайт не выносил.

## 🔓 Открытые user-гейты (что мешает реальному охвату)

**doesitlie (сигналы B+D):**
- [ ] Зарегистрировать **doesitlie.org** (reg.ru/nic.ru/Njalla) — пока внешняя миграция держится (мемори).
- [ ] Прогнать **ChatGPT DR** (3 legal-промпта) → board n=3 → пересобрать build → обновить числа в лонгриде.
- [ ] 2-й аннотатор на gold (≥50 айтемов, inter-annotator κ) — снимает limitation-box.
- [ ] Опубликовать Habr-лонгрид (после re-verify счётчика Charlotin в день публикации) — это и есть закрытие сигнала B.
- [ ] Public repo `theYahia/doesitlie` + GH Pages + HF dataset (по §6 launch-kit).

**yahia-builds:**
- [ ] Домен **yahia.dev** + ре-деплой (Cloudflare Pages CI) — у пользователя есть незакоммиченный WIP от 17.06 (About/Hero/Contact/projects.ts + resume.pdf/og.png), решить что с ним.

## 📦 Артефакты сессии
- `qsearch/doesitlie/docs/habr-longread-ru-2026-06-23.md` (новый, коммит 3f7a09f)
- `yahia-builds/src/data/buildlog.ts` (+2 записи, коммит 1d1ddbc) + пересобранный dist
- `D:/Yahia/active/research/2026-06/idea-catalog-triage-2026-06-23/` (фоновый workflow, в работе)
- этот BURN-LOG

## Главному чату на сверку
- Завести Board-карточку: «Опубликовать Habr-лонгрид doesitlie (сигнал B)» — артефакт готов, ждёт user-гейтов выше.
- После завершения workflow — применить предложения из `30_park_kill.md` к Ideas_tracker.md (single-writer, без гонки).
