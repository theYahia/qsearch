# BURN-LOG — 2026-06-24 v4 (qsearch + doesitlie: verifier module + RaaS + live /verify)

> Режим v4: НЕ переделывать v2/v3 — следующий слой фич + harden + publish-prep до ship. Метод: руками (crown-jewel, correctness-critical) + Workflow fan-out (publish-prep docs, adversarial fact-check). Всё локально, **0 push** (push = user-gate).

VERIFY-STATE-FIRST: baseline снят до правок — doesitlie 23/23, qsearch server 57/57, x-digest 165/165. Ветка `fix/critical-context-and-key-validation`. qsearch LIVE+healthy (Meili+Qdrant ok, Ollama qwen2.5:14b загружен) → verifier/RaaS гонялись end-to-end.

## ✅ Crown jewel — commit `5abd2a9`

### 1. Verifier вынесен в канонический модуль `src/verifier/` (сигнал D — OSS-качество)
- `src/verifier/index.js` + `fetch_content.js` — перенос из `doesitlie/bench/` (фикс путей `../../src/`→`../`). **Одна реализация, три потребителя** (бенчмарк / RaaS / live /verify), ноль дрейфа.
- `doesitlie/bench/{verifier,fetch_content}.js` → thin re-export (`export * from '../../src/verifier/...'`) — `./verifier.js`-импорты в harness.js + validate.mjs не тронуты; **cwd-relative cache (`doesitlie/bench/.cache`) сохранён → published-числа byte-reproducible**. doesitlie **23/23** после extraction.
- `test/unit/verifier/index.test.js` — 5 юнитов (rankParagraphs anchor-boost, judgeLabel, VERDICTS).

### 2. RaaS cited-report generator `src/raas/` (сигнал D — продуктовый клин)
- Реализован весь scaffold M1/M2 (gather/tier/verify/triangulate/pipeline/report/cli). **Триангуляционный гейт = продукт**: claim проходит iff ≥3 независимых (distinct eTLD+1) Supported-источника ≥tierFloor + ≥1 high-authority anchor, иначе low-confidence (никогда silently не дропается). `report.md` (evidence-layer) + `audit.json` (receipt — каждый source/verdict/excerpt перепроверяем) + **<50%-tripwire** «NOT ship-ready».
- Улучшения над scaffold-stub: domain-chain tier-lookup (фиксит seed-mismatch `pubmed.ncbi.nlm.nih.gov`), two-label public-suffix eTLD+1 для независимости, dedupe keeps canonical title + max provenance.
- `test/unit/raas/raas.test.js` — **22 теста** (tiers / gate-boundary / gather-md-parse / verify-adapter-stub / e2e runBrief→report). CLI offline-смоук: `report.md`+`audit.json` + tripwire корректны.
- Импортит `../verifier/index.js` (канонический модуль).

### 3. Live capability: `POST /verify` + `verify_citation` MCP-tool (сигнал D — «trust layer for AI agents»)
- `src/server.js`: `handleVerify` (body {claim,url} → verifyCitation → verdict JSON); validation (claim non-empty ≤4000, url http/https); `fetchContent` SSRF-guarded; verifyCitation никогда не throws. **server 60/60** (57+3 validation-теста).
- `src/mcp.js`: `verify_citation` tool (formatted verdict + evidence + note). Решение по scope: `/raas/report` как публичный HTTP-endpoint **НЕ делал** (build-spec §c: operator/CLI-only by design + abuse-поверхность) — выставлен только bounded-примитив `/verify`.
- README для `src/verifier/` + `src/raas/`.

**Итог тестов: doesitlie 23 + raas 22 + verifier 5 + server 60 = все зелёные. NO .cache pollution** (смоуки под DOESITLIE_NO_CACHE=1).

## ✅ Publish-prep docs — commit `46ef38e` (Workflow `doesitlie-qsearch-publishprep`, fact-checked)
- `content/doesitlie-habr-draft.md` — RU Habr-лонгрид (ЧЕРНОВИК, не опубликован): все числа дословно из `bench/out/all/leaderboard.md` (Gemini 154/Claude 119), κ=0.75 = judge↔human везде, без выдуманного adoption. Supersede'ит старый `doesitlie/docs/habr-longread-ru-2026-06-23.md`.
- `doesitlie/PUBLISH-PLAN.md` — repo-split план theYahia/doesitlie (manifest, bench/.cache commit-vs-ignore, GH Pages, **post-split verifier dependency-chain to break** — после сплита doesitlie standalone, надо vendor'ить src/verifier+fetch+crawl). PLAN ONLY.
- `docs/trust-v2-flip-plan.md` — `QSEARCH_TRUST_FORMULA=v2` flip-decision: 2 дефекта v1 (zero-collapse, linear-vs-diminishing), A/B-доказательство (ρ=0.910, **78% от 1000-URL sample** zero-collapse под v1 — scope уточнён), reversible flip + rollback. Воспроизведено live-прогоном trust_ab.mjs.

## 🔓 User-гейты (qsearch/doesitlie)
- **push** ветки `fix/critical-context-and-key-validation` (есть remote theYahia/qsearch).
- **doesitlie:** домен doesitlie.org · repo-split (по PUBLISH-PLAN) · GH Pages · ChatGPT-DR n=3 · 2-й аннотатор gold · publish Habr (вычитать черновик).
- **trust v2 flip:** ревью flip-doc → `QSEARCH_TRUST_FORMULA=v2` (обратимо).
- **RaaS:** M1-spike (10 outreach / 0 paying за 14дн = kill); seed tier-table операторская ревизия перед платным отчётом.

## ⏸ Deferred (опц., в логе)
Security MED-1 (rerank masterKey fallback) / MED-2 (job-store eviction) / MED-3 (llm_rerank fence) / MED-5 (_urlToId collision — нужна ре-индексация 143k, не вслепую) — следующий спринт.
