# BURN-LOG — 2026-06-24 v5 (SHIP-ENABLEMENT — collapse M3 signal-B/D gates to one-click)

> Режим v5: код продуктов ~готов после v2–v4. Узкое место M3 — НЕ код, а user-гейты (push/deploy/publish/домены/отправка). Задача: собрать машинерию, коллапсирующую гейты до «нажми одну кнопку». Метод: 1 Workflow, **32 агента**, builders → adversarial verify, ~3.4M subagent-tokens, 786 tool-uses, ~46 мин. **0 push / 0 deploy / 0 publish** — всё доведено до последней команды, кнопку жмёт пользователь.
>
> VERIFY-STATE-FIRST (снято до правок): все 4 repo чистые на v4. npm online (PONG 605ms). 3 находки скорректировали план (см. ниже). Это master-лог; sibling-логи: `x-digest/BURN-LOG-2026-06-24-v5.md`, `yahia-builds/BURN-LOG-2026-06-24-v5.md`.

---

## 🟢 ONE-CLICK CHECKLIST (наверху — для каждого сигнала: что готово + точные клики)

### СИГНАЛ B+D — doesitlie (статья Habr + public repo + сайт). Ближе всего к клику.
**Готово до клика:** staging `doesitlie/_publish-staging/doesitlie/` — fresh-tree git, **единый commit `f116afa`, 340 файлов** (66 кода/сайта + **273 pruned cache** + 2 CI workflow). Изоляция от qsearch проверена (`grep ../../` = 0), `node --test` 23/23, `validate.mjs` honesty-gate green (1-in-5 re-derives, 273 receipts trace, κ=0.75), приватное вычищено (strategy-internal/MARATHON/research отсутствуют), 0 секретов. Habr-лонгрид `content/doesitlie-habr-draft.md` — числа сверены byte-for-byte, 0 fabrication, meta-блок (заголовок/Хабы/теги/анонс) готов.
**Клики:**
1. `cd D:/Yahia/active/qsearch/doesitlie/_publish-staging/doesitlie`
2. `gh repo create theYahia/doesitlie --public --source=. --remote=origin --push` *(создаёт внешний repo + push — GUARDRAILED, только пользователь)*
3. GitHub repo → Settings → Pages → Source = **«GitHub Actions»** (⚠️ НЕ «Deploy from branch»/gh-pages — workflow использует artifact-flow `deploy-pages@v4`); Custom domain = `doesitlie.org` (`site/CNAME` уже содержит).
4. Регистратор DNS (вне repo): apex `doesitlie.org` → A `185.199.108-111.153` + AAAA; `www` CNAME → `theYahia.github.io`.
5. После TLS-cert → включить «Enforce HTTPS». Открыть `https://doesitlie.org`, клик по вердикту → audit trail рендерится; `bench-integrity.yml` зелёный на первом push.
6. **Habr:** редактор → вставить тело из `content/doesitlie-habr-draft.md` начиная с `# Когда AI-агент ссылается…` (HTML-meta-блок сверху НЕ вставлять — это шпаргалка) → `<cut>` после TL;DR → заголовок/Хабы/теги из meta → обложка `site/og.png` → **Опубликовать**.
   ⚠️ ссылка на `doesitlie.org` в статье оживёт только после шагов 2–5 — публикуй repo/сайт ДО статьи (или смягчи формулировку про «живую таблицу»).

### СИГНАЛ D — qsearch launch-kit (Show HN + awesome-mcp + demo + comparison).
**Готово до клика:** `docs/launch/SHOW_HN.md` (Trust Mesh positioning, QVAC вычищен, 183 слова + first-comment), `docs/launch/awesome-mcp-PR.md` (ready-to-paste, 9 MCP-tools), `scripts/quickstart.sh` (idempotent one-command), `docs/launch/comparison-post.md` (competitor-факты gated [GAP], 0 fabrication), `scripts/record-demo.sh`+`demo.tape`+README-embed.
**Клики:**
1. **Show HN:** `news.ycombinator.com/submit` → title+url (`github.com/theYahia/qsearch`) → сразу запостить first-comment из `SHOW_HN.md`.
2. **awesome-mcp:** fork `punkpeye/awesome-mcp-servers` → вставить entry-строку в секцию `🔎 Search & Data Extraction` (алфавит, t-буква) → PR.
3. **Demo GIF:** `docker compose up -d && npm start` (рестарт — live-daemon отдаёт 404 на `/verify`) → install vhs (`scoop install vhs ttyd ffmpeg`) → `bash scripts/record-demo.sh` → commit `assets/demo.gif` (README уже ссылается).
4. **Comparison:** dev.to или repo BLOG (по желанию закрыть 6 [GAP]-маркеров competitor-pricing).
5. `git push` ветки (remote `theYahia/qsearch` есть). awesome-selfhosted PR — после age-gate 2026-08-14.

### СИГНАЛ D — x-digest → см. `x-digest/BURN-LOG-2026-06-24-v5.md`
### СИГНАЛ B — yahia-builds → см. `yahia-builds/BURN-LOG-2026-06-24-v5.md` (⚠️ там критичный remote-баг перед push)

---

## 🔎 3 находки VERIFY-STATE-FIRST (анти-фабрикация — план скорректирован)
1. **npm online** → split реально сделал `npm install` + сгенерил lock; cheerio/pdfjs уже в qsearch.
2. **yahia-builds статический** (Astro `output:'static'`) → SSR-adapter `@astrojs/cloudflare` НЕ нужен (промт просил adapter — для статики лишний; static deploy `dist/` правильнее). Verify-агент подтвердил независимо.
3. **yahia-builds remote НЕ настроен** (как x-digest). Ранняя разведка ошибочно показала `origin=qsearch.git` — артефакт гонки параллельных `cd`; перепроверено `git -C` → remote пуст, verify-агент подтвердил. Перед push нужен `gh repo create theYahia/yahia-builds` + `branch -M main`.

---

## ✅ Поток 1 — doesitlie (сигнал B+D): repo-split + cache-prune + CI + Habr

| Артефакт | Verdict | Детали |
|---|---|---|
| **split** (`doesitlie/scripts/split-to-staging.mjs`) | ✅ PASS | Идемпотентный wipe-and-rebuild по PUBLISH-PLAN §1/§5. Vendor 5 транзитивных файлов под mirrored `src/`, 2 import-edit + ⚠️highest-risk `CACHE_DIR 'doesitlie/bench/.cache'→'bench/.cache'` (verify подтвердил `index.js:174`). package.json private:false + exact-pinned cheerio 1.2.0/pdfjs 6.0.227. **Verify поймал 2 утечки builder'а** (ложно заявил «scrubbed»): RaaS-монетизация в doc-comment `bench/verifier.js` (то, что PUBLISH-PLAN вырезал из ROADMAP) + broken cross-repo link `bench/README.md:48` — исправлено, amended. |
| **prune** (`doesitlie/scripts/prune-cache.mjs`) | ✅ PASS | Детерминированно реконструирует 273 cache-key из `audit.json` по формуле `sha256(v7\|deepseek:deepseek-chat\|url\|claim)` (прочитана из `verifier/index.js:186`, не по памяти). **972 → 273 copied, 0 missing.** Прунь к DeepSeek (не ollama — иначе −183 verdict). `validate.mjs` green после. |
| **ci** (staging `.github/workflows/`) | ✅ PASS | `deploy-pages.yml` (official Pages artifact-flow: build→upload-pages-artifact→deploy-pages@v4). `bench-integrity.yml` + `npm ci` (PUBLISH-PLAN §4). YAML провалидированы. ⚠️ Pages source = «GitHub Actions», НЕ gh-pages branch. |
| **Habr** (`content/doesitlie-habr-draft.md`) | ✅ PASS | freshness-секция v4 (canonical verifier / RaaS-гейт / live `/verify`). **0 fabrication** — каждое число сверено с `leaderboard.md`+`site/data.json` (Gemini 154/Claude 119, 1-in-5, κ=0.75/0.737). Внешние факты (Oregon $109,700, Florida Rule 2.515(d)(2), 1200+ дел) verify нашёл в `bench/README.md` — НЕ выдуманы. Meta-блок + кат + obложка готовы. |

## ✅ Поток 2 — qsearch launch-kit (сигнал D)

| Артефакт | Verdict | Детали |
|---|---|---|
| **SHOW_HN.md** | ✅ PASS | QVAC полностью вычищен (grep `@qvac/sdk`/`Qwen3-0.6B`=0), Trust Mesh positioning, 183 слова, title 73<80. Trust-формула трассирована к `trust.js:8`. |
| **awesome-mcp-PR.md** | ✅ PASS | **Verify 3 фикса:** добавлен 9-й MCP-tool `verify_citation` (builder скопировал stale README 8-tool list); bind `0.0.0.0`→`127.0.0.1` (README overstated exposure, код = loopback fail-fast). Формат сверен с live `punkpeye/awesome-mcp-servers` CONTRIBUTING. |
| **quickstart.sh** | ✅ PASS | 0 дефектов. `set -euo pipefail`, не перезаписывает `.env.local`, health на :8080, prereq-проверки реальны (docker info/compose v2-v1/node≥20). |
| **comparison-post.md** | ✅ PASS | Таблица cell-for-cell из README (8×6). **0 fabrication** — каждый competitor-факт либо из README, либо `[GAP: verify]`. Perplexity (нет в README) — только category-statement, без выдуманных чисел. AI-slop = 0. |
| **demo recorder** | ✅ PASS | vhs-first/asciinema-fallback, 3-act сценарий (`/health`→`/sweep`→`/verify`). **Verify поймал фабрикацию:** narration «Google» при реальном SearXNG-конфиге (Bing/DDG/Brave/Qwant) — исправлено. GIF = [GAP] (нет vhs/TTY — user record). |

---

## 🔓 User-гейты (qsearch + doesitlie) — то, что осталось пользователю
- **doesitlie:** `gh repo create theYahia/doesitlie` + push + Pages(GitHub Actions source) + DNS `doesitlie.org` + Habr publish (см. one-click §B+D).
- **qsearch:** push ветки `fix/critical-context-and-key-validation` (remote есть); Show HN submit; awesome-mcp PR; demo GIF record; awesome-selfhosted PR после 2026-08-14.

## 📋 Открытые [GAP] (честно, не выдумано)
- doesitlie repo-split: `.gitattributes` (`* text=auto eol=lf`) nice-to-have для CRLF-нормализации public repo (косметика). `npm run build` (site regen) не гонялся — `site/` это уже-собранный committed артефакт. `crawl4ai==0.8.6` — optional runtime (worker печатает clean error если нет).
- doesitlie CI: action major-tags (`deploy-pages@v4` и т.д.) не pinned к SHA — verify при первом run.
- qsearch demo: live-daemon на :8080 stale (404 на `/verify`) — рестарт перед записью; SearXNG сейчас отдаёт мало engines — для богатого кадра поднять healthy SearXNG.

## ⚙️ Локальные коммиты
v5-работа закоммичена локально в каждом repo (qsearch / x-digest / yahia-builds), **без push** (push = user-гейт). Staging doesitlie = отдельный fresh-tree repo (`_publish-staging/`, gitignored в qsearch), commit `f116afa`.
