# assets

Static assets referenced by the project README.

- `demo.gif` — terminal demo (quickstart → multi-engine search → citation verify).
  **Not yet recorded** — produce it with `bash scripts/record-demo.sh` (needs a
  live TTY + qsearch running on `:8080`). Until then the README's demo block shows
  a broken-image icon. See [`docs/launch/README-demo-embed.md`](../docs/launch/README-demo-embed.md).
- `demo.cast` — optional asciinema recording (only if you record with asciinema).
